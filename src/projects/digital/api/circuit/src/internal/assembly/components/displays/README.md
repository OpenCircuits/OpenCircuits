# Display Assemblers (`internal/assembly/components/displays`)

This directory contains the view-layer assembly logic for complex, multi-segment displays like the 7-Segment, BCD, and ASCII displays. These assemblers handle the intricate rendering of individual lit/unlit segments based on the simulation state.
