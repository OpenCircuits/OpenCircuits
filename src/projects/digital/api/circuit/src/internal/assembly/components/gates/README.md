# Gate Assemblers (`internal/assembly/components/gates`)

This directory contains the view-layer assembly logic for foundational logic gates (AND, OR, XOR, NAND, NOR, XNOR, NOT, BUF). It handles dynamically sizing the gates based on the number of inputs and rendering their standard ANSI/IEEE distinctive shapes using Bezier and Quad curves.
