# Flip-Flop Assemblers (`internal/assembly/components/flipflops`)

This directory contains the view-layer assembly logic for flip-flops (D, T, JK, SR). It defines their rectangular shapes, pin locations (including clock, preset, clear), and labels.
