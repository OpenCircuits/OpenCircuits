# Latch Assemblers (`internal/assembly/components/latches`)

This directory contains the view-layer assembly logic for latches (D Latch, SR Latch). It defines their block shapes, Enable/Data pin locations, and visual representation.
