class Action {
    constructor() {
        this.context = getCurrentContext();
        // Anytime an action is performed, user should need to save
        saved = false;
    }
    undo() {
    }
    redo() {
    }
}
