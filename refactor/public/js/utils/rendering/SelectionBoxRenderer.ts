import {Renderer} from "./Renderer";
import {Camera} from "../Camera";
import {SelectionBox} from "../SelectionBox";

export var SelectionBoxRenderer = (function() {

    return {
        render(renderer: Renderer, camera: Camera, box: SelectionBox) {
            
        }
    };
})();
